services:
  museko:
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "5000:5000"

    volumes:
      - .:/app

    environment:
      FLASK_ENV: development

    stdin_open: true
    tty: true

    restart: unless-stopped